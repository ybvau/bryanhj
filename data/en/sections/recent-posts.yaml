# section information
section:
  name: Recent Posts
  id: recent-posts
  enable: true
  weight: 6
  showOnNavbar: true
  hideTitle: true
  # Can optionally hide the title in sections
  # hideTitle: true

# no additional configuration is required
